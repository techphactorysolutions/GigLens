{
  "name": "giglens-pwa",
  "version": "4.4.0",
  "private": true,
  "description": "GigLens 4.4 local-first PWA with a functional-minimalist mobile UI, calendar history, screenshot timestamps, OCR learning, analytics, backups, and decision tracking.",
  "scripts": {
    "test": "python -m unittest discover -s tests",
    "syntax": "node --check app.js && node --check service-worker.js && node --check 404.js",
    "smoke": "node tools/smoke-startup.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
