{
  "name": "giglens-pwa",
  "version": "4.1.0",
  "private": true,
  "description": "GigLens with embedded in-app branding, local OCR correction learning, repaired screenshot scanning, simplified mobile UI, local-first analytics, backups, and decision tracking.",
  "scripts": {
    "test": "python -m unittest discover -s tests",
    "syntax": "node --check app.js && node --check service-worker.js && node --check 404.js",
    "smoke": "node tools/smoke-startup.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
