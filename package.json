{
  "name": "giglens-pwa",
  "version": "4.5.0",
  "private": true,
  "description": "GigLens 4.5 local-first PWA with calendar monthly analytics, screenshot timestamps, OCR learning, functional-minimalist UI, backups, and decision tracking.",
  "scripts": {
    "test": "python -m unittest discover -s tests",
    "syntax": "node --check app.js && node --check service-worker.js && node --check 404.js",
    "smoke": "node tools/smoke-startup.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
