{
  "name": "driveledger-pwa",
  "version": "3.9.0",
  "private": true,
  "description": "DriveLedger premium local-first driver command center with earnings, mileage, profit, tax, platform and zone analytics, app-specific OCR capture, paused-shift tracking, and persistent order decision tracking.",
  "scripts": {
    "test": "python -m unittest discover -s tests",
    "syntax": "node --check app.js && node --check service-worker.js",
    "smoke": "node tools/smoke-startup.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
